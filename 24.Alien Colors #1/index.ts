// Alien Colors #1

// Variable to store the alien's color
let alien_color: string;

// Assigning a value to the alien_color variable
alien_color = 'green';

// Version that passes the if test (output: "Player just earned 5 points.")
if (alien_color === 'green') {
    console.log("Player just earned 5 points.");
}

// Version that fails the if test (no output)
if (alien_color !== 'green') {
    // No code executed
}
