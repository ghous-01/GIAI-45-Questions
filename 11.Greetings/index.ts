const names: string[] = ["Adnan", "Taha", "Ahmed", "Jazil"];
const greetingMessage: string = "Hello, would you like to join us for dinner tonight?";
names.forEach(name => {
    console.log(`Hi ${name}, ${greetingMessage}`);
});
