function make_shirt(size, message) {
    console.log("The shirt size is ".concat(size, " and the message printed on it is: \"").concat(message, "\"."));
}
// Call the function with a size and a message
make_shirt('XL', 'Pakistan Zindabad');
