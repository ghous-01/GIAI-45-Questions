let colors: string[] = ["Red", "Green", "Blue"];

// Intentional error: Accessing an invalid index
console.log(colors[3]); // This line will produce an array index error

// Correcting the error by accessing a valid index
console.log(colors[2]); // This line accesses the last element of the array
